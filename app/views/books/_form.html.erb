<%= form.fields_for :tags do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
<%= end %>

<div class="actions">
  <%= form.submit %>
</div>

<div class="tags">
  <%= form.fields_for :tags do |f| %>
    <%= f.label "タグ" %>
    <%= render "tag_fields", f: f %>
  <% end %>
  <div class="links">
    <%= link_to_add_association "タグを追加", form, :tags %>
  </div>
</div>
